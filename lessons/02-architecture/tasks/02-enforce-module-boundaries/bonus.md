## Bonus

- Add a rule so `common-components` can only be imported by shell or book, not by other shared modules.
- Extend the rules when you add a new domain (e.g. `runtime-configuration` in a later task).
